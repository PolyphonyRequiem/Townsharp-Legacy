namespace Townsharp.Servers
{
    public class ServerSnapshot : Server
    {
        public ServerSnapshot(ServerId serverId, ServerStatus serverStatus) : base(serverId, serverStatus)
        {

        }
    }
}
